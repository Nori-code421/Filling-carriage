# 晨盤 Brunch Atlas｜整體視覺設計

## 1. 設計主張

「深藍早晨裡的一張橘色點餐單」。網站不走常見的米白咖啡館模板，而以墨水藍作為舞台，搭配鮮橘與蛋黃色，形成像獨立飲食雜誌般清楚、有性格的視覺。大字標題、編號索引與細線欄位共同建立「圖鑑」感；實拍食物影像負責帶出溫度。

## 2. 色彩系統

| Token | 色碼 | 用途 |
| --- | --- | --- |
| `--ink` | `#071B2E` | 主背景、標題、深色面板 |
| `--ink-2` | `#0B2944` | 深色層次 |
| `--cream` | `#F4EFE3` | 網站底色 |
| `--paper` | `#FFFAF0` | 卡片與閱讀面 |
| `--orange` | `#FF5A1F` | 主強調、編號、互動狀態 |
| `--yellow` | `#FFC857` | 次強調、焦點外框 |
| `--sky` | `#92D5F5` | 預留的冷色輔助色 |

色彩規則：橘色只用於「下一步、索引與重點」，避免整頁同時出現過多高彩度元素。正文一律維持深色高對比。

## 3. 字體與層級

- 顯示字：`Arial Black` 搭配系統繁中字體，粗重、緊縮、略帶海報感。
- 內文字：`Noto Sans TC`、`PingFang TC`、`Microsoft JhengHei` 的系統降級鏈。
- H1：`clamp(3.2rem, 7.1vw, 7.1rem)`，行高 `0.95`。
- H2：`clamp(2.7rem, 5vw, 5.2rem)`，行高 `0.98`。
- 正文：最小 `1rem / 16px`，行高 `1.65`。
- Metadata：僅使用於非必要的英文分類與期號，可降至 `0.68–0.76rem`。

## 4. 版面規則

- 桌機：12 欄網格，卡片各跨 4 欄；第二張卡片下沉，形成編輯式節奏。
- 平板：卡片跨 6 欄，第三張置中跨 8 欄。
- 手機：單欄排列，移除裝飾性錯位，確保閱讀順序穩定。
- 內容最大寬度：`1440px`。
- 頁面水平留白：`clamp(1.25rem, 5vw, 5.5rem)`。
- 邊角以方正為主；僅圖鑑編號與關閉按鈕使用圓形，避免介面過度柔化。

## 5. 圖像風格

- 俯拍、清晰自然光、真實食物質地。
- 背景使用深墨藍；餐盤可帶鈷藍與陶土橘。
- 保留可承載標題的負空間。
- 不使用文字浮水印、品牌標誌或過度泛黃濾鏡。
- 同一張主視覺可透過不同 `object-position` 裁切，形成條目縮圖的一致系列感。

## 6. 元件規範

### 圖鑑卡片

- 結構：圖片／編號 → 分類與英文名 → 標題 → 摘要 → 標籤 → 展開按鈕。
- Hover：上移 `6px` 並增加陰影；縮圖緩慢放大。
- 手機取消錯位，但保留輕微上移回饋。

### 條目對話框

- 使用原生 `<dialog>`，支援 `Esc` 關閉與鍵盤焦點。
- 頂部為深色圖像封面，下方依內容陣列渲染區塊。
- 內容類型使用 `type: text | video | html | slides`；新增類型時，在 `app.js` 的 `renderers` 對照表加入渲染器。

### 互動狀態

- 所有鍵盤焦點使用 `3px` 蛋黃色外框。
- 互動按鈕至少 `44 × 44px`。
- 尊重 `prefers-reduced-motion`，降低所有動畫。

## 7. 新增條目資料格式

在 `app.js` 的 `entries` 陣列新增物件：

```js
{
  id: "unique-slug",
  number: "04",
  title: "條目名稱",
  english: "ENGLISH NAME",
  category: "分類 · 特性",
  summary: "卡片摘要",
  tags: ["標籤一", "標籤二"],
  mediaPosition: "50% 50%",
  blocks: [
    { type: "text", label: "NOTE", title: "段落標題", body: "內容" },
    { type: "video", label: "VIDEO", title: "影片標題", url: "video.mp4" },
    { type: "html", label: "HTML LAB", title: "互動作品" },
    { type: "slides", label: "DECK", title: "簡報", slides: [] }
  ]
}
```

## 8. 無障礙與響應式底線

- 圖像皆有替代文字；純裝飾裁切使用空白 `alt`。
- 主要區塊皆有語意標籤與標題關聯。
- 手機最小支援寬度為 `320px`；內容不可水平溢出。
- 字體放大至 200% 時，導覽、卡片與對話框仍可閱讀與操作。
