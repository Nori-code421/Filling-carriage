# 晨盤 Brunch Atlas

零相依的響應式靜態網站。直接以瀏覽器開啟 `index.html`，或使用任一靜態伺服器預覽。

## 檔案

- `index.html`：頁面語意結構
- `styles.css`：設計 token、版面與響應式樣式
- `app.js`：圖鑑資料、內容渲染與互動
- `design.md`：整體視覺設計與擴充規範
- `assets/brunch-hero.png`：原創主視覺

新增條目時，只需要在 `app.js` 的 `entries` 陣列加入一筆資料。支援文字、影片、HTML 作品與簡報內容區塊。
